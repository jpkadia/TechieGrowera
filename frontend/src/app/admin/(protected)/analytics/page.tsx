import { AnalyticsDashboard } from '@/components/admin/analytics';

export const metadata = { title: 'Visitors & Traffic — Admin' };

export default function AnalyticsPage() {
  return <AnalyticsDashboard />;
}
